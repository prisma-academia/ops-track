const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); prisma.transport.groupBy({by: ['productType'], _sum: {litersCarried: true}}).then(console.log).finally(() => prisma.$disconnect());
